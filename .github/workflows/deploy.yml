name: Deploy on Push

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull and redeploy
        shell: powershell
        run: |
          C:\Calibration\deploy-package\deploy.ps1
