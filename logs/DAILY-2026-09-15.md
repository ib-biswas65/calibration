# 2026-09-15

## 17:xx — Reviewed current implemented state of the app

- Read-only exploration, no code changes. User wanted a summary of what's currently implemented.
- GitNexus index was fresh (indexed commit `c08b1e3` matches HEAD), so used `context`/`clusters` resources plus `docs/ARCHITECTURE.md` instead of grepping.
- Confirmed: ITE Calibration is a FastAPI + React (Vite/TS) dashboard that turns uploaded reference-logger CSVs + a calibration workbook into Japanese calibration certificates (.docx), with run lifecycle draft→processing→complete/failed, auth, audit trail, user management.
- Noted the repo's rocky history: broken 2026-07-09 merge (`e8131b9`) lost code, recovered across three fix commits on 2026-08-14 (`9bcc0ef`, `d55003a`, `3a78a4b`), followed by cleanup commit `c08b1e3` (also 2026-08-14) which is current HEAD.
- Gap found: PROGRESS.md's macro log only had the 2026-08-22 "restored onto new machine" line — it never recorded what the 2026-08-14 recovery commits actually did. Backfilled that into PROGRESS.md this session.

## 17:xx — Wrote Windows PC migration runbook

- User needs to migrate the live prod stack (app + Postgres DB + certificate volume) to a different Windows PC next week.
- Dispatched an agent on the `fable` model (user asked specifically to use Fable) to read `docs/DEPLOYMENT.md`, `docs/ARCHITECTURE.md`, `deploy-package/{setup-windows.ps1,backup-windows.ps1,docker-compose.yml,.env.example}`, `register-task.ps1`, and `.gitignore`, and produce the migration plan.
- Key findings baked into the runbook:
  - `deploy-package/images/`, `cal_data.tar.gz`, and all `.env` files are gitignored — they exist only on the currently-running machine. Rebuilding from git instead of copying the exact running images is what caused the 2026-08-14 incident (docs/DEPLOYMENT.md) — so the runbook `docker save`s the exact running images and verifies image IDs at each step, never rebuilds.
  - `backup-windows.ps1`'s `pg_dump ... > file` redirect risks UTF-16 re-encoding in PowerShell 5.1, which would corrupt the Japanese text in certificate/batch data. The runbook instead does `pg_dump -f` inside the container + `docker cp`, never piping SQL through PowerShell.
  - `setup-windows.ps1` (the existing "first-time setup" script) restores the stale committed `seed/db_full.sql`, not live data — not usable as-is for this migration, so the runbook adapts its structure manually instead.
- Wrote the full runbook to `docs/MIGRATION.md` (capture steps on old PC, restore steps on new PC, cutover plan, rollback, risk table) and added a pointer to it from `docs/ARCHITECTURE.md`'s Environments section.
- Left 7 open questions in the doc that only the user can answer (IP/hostname handling, whether both PCs are online simultaneously during the move, the actual downtime window, whether `C:\Calibration` is confirmed clean/correct on the old PC, whether new-PC test runs during fallback count as real data, whether to fix the `backup-windows.ps1` encoding risk now, and whether to ever re-enable the dormant GitHub Actions self-hosted runner on the new PC — recommended not to).
- Committed `docs/MIGRATION.md` + `docs/ARCHITECTURE.md` pointer as `9cca1d6`. Not pushed.
- Note: GitNexus index is now one commit stale (last indexed `c08b1e3`); not refreshed this session since only docs changed.

## 18:xx — Reworked migration plan: no fallback, one-shot move

- User clarified a critical constraint: the old PC will be completely unreachable once the new one arrives (no remote access, no one on-site), and there's a multi-day gap between capturing the backup bundle and actually setting up the new PC. Confirmed via AskUserQuestion rather than assumed.
- This invalidates the original plan's safety nets (rollback to old PC, 2-week fallback period, side-by-side verification) — rewrote `docs/MIGRATION.md` accordingly.
- Key addition: new mandatory section 2a — dry-run restore the full bundle onto a spare Windows machine with Docker Desktop *while the old PC is still available*, since that's the only point where a bad capture is still fixable. Passing this dry run is now the real go/no-go gate, not the cutover itself.
- Also added: two independently hash-verified transfer copies (single-copy loss is now unrecoverable), timing guidance (rehearsal capture early to validate procedure, final capture as close as possible to the old PC's departure), and rewrote the Rollback section to state plainly there isn't one anymore — only re-running restore from the bundle.
- Committed as `371bff2`. Not pushed.
- Biggest open risk flagged to user: whether a spare Windows machine with Docker Desktop is actually available for the mandatory dry run — without it the plan has no safe validation step before the point of no return.

## 19:xx — Simplified migration plan: fresh app build instead of image copy

- User clarified two things via AskUserQuestion: (1) this Mac (Docker via Colima, confirmed present) can serve as the dry-run machine — no spare Windows box needed; (2) only the DB is confirmed to need migrating as live data — the app itself should be rebuilt fresh on the new PC, not shipped as exact baked images, because they also want to "refine the application" (bug fixes, new features, ops cleanup, UI redesign) before the move.
- This flips the core risk model from `docs/MIGRATION.md`'s previous version: rebuilding from git used to be unsafe (2026-08-14 incident, git/deployed image had diverged); it's safe now because git HEAD is confirmed clean and the plan explicitly wants the new PC to run improved code, not a byte-copy of what's running today.
- Rewrote `docs/MIGRATION.md`: dropped all `docker save`/`load` image steps; added a step to tag the exact reviewed git commit the new PC will build from (`windows-migration-<STAMP>`); new PC build step is `docker build` + `docker pull postgres/nginx` instead of loading tars; `.env` gets fresh secrets on the new PC instead of copying the old password/JWT secret; dry run (2a) now runs on this Mac, with an explicit note that arm64-here vs amd64-there means it validates the Dockerfiles/dependencies/restore procedure, not the exact platform build. Added an open question about whether the new PC will have internet access for `docker build` (needed for the default path; fallback is building elsewhere and transferring images).
- Committed as `0db5054`. Not pushed.
- **Explicitly did not start on "refine the application."** Bug fixes + features + ops cleanup + UI redesign is underspecified (which bugs, which features, what redesign direction) and large enough to need its own scoping/brainstorming pass before any code changes — flagged this to the user rather than guessing. This is the natural next-session starting point.

## 20:xx — Opened GH issue #2 for the DB dry-run capture; collaborator ran it and found a live production bug

- User asked to file the "how to migrate the DB for now" instructions as a GitHub issue so a collaborator could follow them on the production PC. Created https://github.com/ib-biswas65/calibration/issues/2 with the Part A capture commands.
- Collaborator (`thisisartman`) ran it on the production PC and reported back in comments: dump captured cleanly (685,803 bytes, verified not UTF-16, Japanese text intact), restore-tested in a throwaway `postgres:16-alpine` container — all 10 tables match live row counts, checksums match on `loggers`/`calibration_runs`/`logger_results`/`audit_log`. File handed over via USB with SHA256 verification.
- They flagged two real problems while doing this:
  1. `docs/MIGRATION.md` had never been pushed to GitHub (I'd only committed locally all session) — they had only the issue text to work from.
  2. `Format-Hex -Count 4` (the sanity-check command I wrote) doesn't exist in Windows PowerShell 5.1, the production PC's actual shell.
- They also opened **issue #3**, a genuinely broken production backup: the nightly `backup-windows.ps1` preflight `try/catch` around `docker version` never fires in PowerShell 5.1 (native command failures only set `$LASTEXITCODE`, they don't throw) — so when Docker Desktop was found stopped on the PC, the backup silently wrote an empty `db.sql` instead of aborting. Separately, line 49's `pg_dump ... > file` redirect is the UTF-16-corruption risk already flagged in MIGRATION.md, now confirmed live — every nightly backup before today has broken Japanese text. The issue #2 test dump was, at that point, the only clean DB backup in existence.
- Applied their tested fixes to `deploy-package/backup-windows.ps1`: preflight now checks `$LASTEXITCODE` directly; DB dump now uses `pg_dump -f` inside the container + `docker cp` (byte-safe) with `$LASTEXITCODE`/empty-file checks instead of the PowerShell redirect; added a `$LASTEXITCODE` check after the certificate-volume `docker run`. Also fixed the `Format-Hex -Count` → `Format-Hex | Select-Object -First 1` correction in `docs/MIGRATION.md`.
- Discovered while preparing to push: `origin/main` had moved (3 new commits: certificate template fixes + a new `docs/CHANGE_LOG.md`, from someone/something else) and a stray branch `claude/certificate-input-updates-8b9t8m` appeared (already fully merged into main, no unique commits — left untouched). No file overlap with this session's changes. Rebased cleanly onto the new `origin/main` (8 commits, no conflicts).
- **Push held for explicit user confirmation** (shared branch, collaborator actively working off it) — asked "ready to push?" before executing. Not yet pushed as of this handoff.
