# 2026-09-16

## 00:xx — Fixed critical certificate-correctness bug + added validation feature (TDD)

- User asked to scope "refine the application" (bug fixes/features/ops/UI). Ran two parallel read-only audits (fork agents): a systematic bug audit of `apps/api`/`apps/web`, and an ops/deploy-package audit. Both surfaced real, unrelated findings — see below.
- **Critical finding acted on immediately (with user approval, after a brainstorming-skill design pass)**: `apps/api/ite_api/calibration/matcher.py:103`'s step-3 fallback returned the setpoint target temperature as a fake reference reading whenever nothing in the window was within tolerance — that fabricated value could print on certificates and get used to compute the pass/fail verdict itself. Zero test coverage on that exact path (confirmed by the audit).
- Approved design: matcher returns "no match" instead of fabricating; a logger whose data can't be matched gets `verdict="invalid"` + `failure_reason`, no cert generated, run continues for other loggers; run status becomes `partial` (mixed) or `failed` (all invalid) instead of always `complete`.
- Implemented via strict TDD (test-driven-development skill) across `matcher.py`, `ref_loader.py`/`cal_loader.py` (now raise instead of silently returning empty data on unparseable files), `LoggerResult.failure_reason` (new column + migration `0008`), `routes/runs.py::_do_process` (restructured to check matches before generating a cert), `routes/runs.py`/`routes/overview.py` pass-rate SQL (exclude invalid from denominator), and frontend (`StatusPill` invalid/partial styles, `RunDetailPage` inline failure reasons + invalid filter, `HistoryPage` partial filter option).
- **Environment work needed to make TDD possible at all**: no local `.venv` existed for `apps/api` (created one, `pip install -e ".[dev]"`); `apps/web` had no `node_modules` (ran `npm install`). Both were first-time setups on this machine.
- **Found along the way, reported but NOT fixed (scope discipline per advisor consult)**:
  1. CI has been red on the **lint** step on every push since 2026-08-14 — the Test step has never actually run.
  2. `ite_api/main.py:50` hardcodes `Config("/app/alembic.ini")` — only resolves inside the Docker image (WORKDIR `/app`), so the `client` TestClient fixture can't boot the app outside a container. Worked around by calling route functions directly against `db_session` (a real testcontainers Postgres) instead of going through HTTP — documented in the new test files' docstrings.
  3. Separately, `tests/conftest.py`'s `engine` fixture does `Base.metadata.create_all()` while the app's real startup does `alembic upgrade head` against the same DB — these collide (`DuplicateTable`) whenever both run in one test session, meaning route tests using `client` have likely never actually passed regardless of environment.
  4. `tests/test_telemetry.py` fails — `opentelemetry` is imported but not a declared dependency in `pyproject.toml`.
  - All four handed to the user as ops-cleanup candidates, not touched.
- Verified: 29/29 backend tests green (`pytest tests/calibration tests/routes/test_run_processing.py tests/routes/test_run_summary_stats.py tests/routes/test_overview_stats.py`), 11/11 frontend tests green (`npm test`), clean `tsc --noEmit`, clean `npm run build`, lint-clean on every file touched (ran `ruff check` scoped to just the changed files, since the repo has 22 pre-existing unrelated lint errors blocking CI — didn't touch those, would have buried this diff).
- Committed `4911ab5`, pushed to `origin/main` (user explicitly asked to push this time).
- Not yet started: the "features" and "ops cleanup" and "UI redesign" buckets from the original four-part scoping — only the "bug fixes" bucket's highest-priority item was actioned. User wants to discuss what's next in detail.

## 10:xx — Full ITE brand redesign, shipped and verified live

- Ran the `ui-design-system` pipeline. No `DESIGN.md` existed; discovered the app's palette was a placeholder — `tokens.css` literally commented it "Officevibe brand palette", not ITE's real branding.
- Built a 3-screen mockup Artifact (Overview/History/Run Detail) in ITE's real identity (green droplet/leaf mark, red/amber/blue/green accent quartet — confirmed live on icebattery.jp and matched in 2 other shipped ITE products) — user approved, said "continue with this."
- Implemented directly in the app: `DESIGN.md` (new), `tokens.css` retint (kept all variable names, changed values only — cascaded cleanly through ~18 CSS modules with minimal touching), `index.html` fonts (Instrument Sans + JetBrains Mono replacing Inter), `AppShell`/`Sidebar` (ambient gradient ground, dark gradient sidebar, brand mark + quartet stripe, lucide icons per nav item), `StatusPill` (dot-style pills), glass-surface (`backdrop-filter: blur`) additions to `StatTile`/`DataTable`/`ConfirmDialog`/`FileDropZone`.
- **Verified live, not just built**: spun up the real Docker dev stack (`infra/docker-compose.yml`), seeded realistic calibration data directly via SQL, logged in via Playwright, screenshotted Overview/History/Run Detail/New Calibration. Confirmed yesterday's backend feature (invalid results, partial status) renders correctly end-to-end for the first time.
- **Found a second, unrelated critical bug while doing this verification**: migration `0008`'s revision id (`0008_add_logger_result_failure_reason`, 38 chars) exceeds Alembic's `alembic_version.version_num` column limit (VARCHAR(32)) — the final `UPDATE alembic_version` failed, and because Alembic runs a fresh DB's whole upgrade chain in one transaction by default, this silently rolled back ALL 8 migrations, leaving an empty schema despite log output claiming success. Renamed to `0008_add_failure_reason` (23 chars), reverified clean against a fresh Postgres. This would have hit next week's Windows PC migration directly.
- Ran the user's own `dev-pipeline` skill against this session's work to find gaps, per their request. Found: a real WCAG contrast bug in the just-shipped redesign (several status colors illegible as text, `--color-feedback-yellow` at 1.51:1) — fixed by darkening 5 tokens, every text/bg pair reverified ≥4.5:1 by computing actual ratios, not eyeballing. Also surfaced process gaps: 15+ direct pushes to `main` all session (the exact anti-pattern the skill warns about by name), no branch protection (repo confirmed **public**, so nothing blocks fixing this), CI red on lint since 2026-08-14, no secrets-scan/style-check/Dependabot.
- Dispatched Fable to write a full phased implementation plan for everything found (5 more known bugs, ops/deploy issues, CI/process gaps, the unresolved "Upcoming" report, remaining design polish) → `docs/superpowers/plans/2026-09-16-post-redesign-implementation-plan.md`. Walked through all 10 of its open questions with the user; answers recorded directly in that plan file's "resolved" section (PR-per-change adopted for `apps/`, fix lint now, cert_no unique-among-valid-only, commit agent config, flag-not-block stale certs, delete dormant auto-deploy, keep+fix staging creds, blanket -9h historical date correction confirmed safe, full 5-page design-polish scope + Login gets the brand mark).
- **Started executing Phase 0 with subagents, per user's request for concurrency.** Split the previously-uncommitted mixed working tree into two branches (`fix/migration-0008-revision-length`, `feat/ite-brand-redesign` — worktree-isolated agents can only see committed refs, not working-tree diffs, so this had to happen before dispatching). Committed config files (`CLAUDE.md`/`AGENTS.md`/`.claude/skills`, inspected first — confirmed benign GitNexus output, no secrets) directly to `main`. Dispatched two parallel worktree agents to independently re-verify and open PRs for each branch (not merge — that's still pending review). **Both in flight as of this handoff — see HANDOFF.md.**
- **Found unmerged prior work while checking for branch conflicts, before it caused a collision**: `origin/claude/calibration-ui-overhaul-plan-6uo67q` (dated 2026-08-17) is a detailed prior planning doc that already root-caused the user's "Upcoming calibrations shows nothing" report: `Logger.next_due_at` is never written by any automated code path anywhere in the system (only a manual PATCH endpoint touches it), so the column is NULL fleet-wide and `UpcomingPage.tsx`'s `next_due_at != null` filter excludes everything. That plan also independently designs the fix (a `recompute_logger_schedule` helper keyed on `testing_start`, called from every history-changing path, plus a backfill script) and covers cert_no uniqueness (overlaps today's Phase 2.1) plus other unscoped work (archive/restore, audit trail, Overview overhaul, PDF export) with its own 9-phase build order.
- Also found `origin/add-otel-instrumentation`, committed *today* under the user's own git identity — opt-in OpenTelemetry tracing wired to what looks like a local Monoscope instance, safe-fail, zero overhead when unconfigured. Explains the "`opentelemetry` undeclared dependency" mystery from earlier in the day: it's not a bug on `main`, it's unmerged work sitting on this branch.
- **Two open questions posed to the user, not yet answered**: (1) reconcile the old UI-overhaul plan into today's plan, or keep separate? (2) is the otel branch active/theirs, ready for review, or still in progress on their end?